# 🏗️ Architecture Diagram - GoReserve

## Class Hierarchy

```
                    Transport (Abstract)
                         |
        ┌────────────────┴────────────────┐
        |                                 |
      Bus                               Train
   (Concrete)                        (Concrete)
   - busType                         - trainClass
   - calculatePrice()                - calculatePrice()
   - getTransportType()              - getTransportType()
```

---

## Application Layers

```
┌─────────────────────────────────────────────────────┐
│              TicketReservationApp                   │
│              (Presentation Layer)                   │
│  - Menu-driven interface                            │
│  - User input handling                              │
│  - Display formatting                               │
└─────────────────────────┬───────────────────────────┘
                          │
                          │ calls
                          ▼
┌─────────────────────────────────────────────────────┐
│              ReservationSystem                      │
│              (Business Logic Layer)                 │
│  - bookTicket()                                     │
│  - cancelTicket()                                   │
│  - searchTransports()                               │
│  - saveData() / loadData()                          │
└─────────────────────────┬───────────────────────────┘
                          │
                          │ manages
                          ▼
┌─────────────────────────────────────────────────────┐
│           Collections & Data Classes                │
│              (Data Layer)                           │
│  - HashMap<String, Transport>                       │
│  - HashMap<String, User>                            │
│  - ArrayList<Ticket>                                │
│  - User, Ticket, Transport objects                  │
└─────────────────────────┬───────────────────────────┘
                          │
                          │ persists to
                          ▼
┌─────────────────────────────────────────────────────┐
│              File System (Persistence)              │
│  - tickets.dat                                      │
│  - users.dat                                        │
│  - transports.dat                                   │
└─────────────────────────────────────────────────────┘
```

---

## Data Flow - Booking a Ticket

```
User Input (TicketReservationApp)
       │
       │ 1. getUserInput()
       ▼
Get Transport ID & Seat Number
       │
       │ 2. bookTicket(userId, transportId, seatNumber)
       ▼
ReservationSystem
       │
       ├─ 3. Validate user exists
       ├─ 4. Validate transport exists
       ├─ 5. Check seat availability
       │
       │ 6. transport.bookSeat(seatNumber)
       ▼
Transport (polymorphic call)
       │
       ├─ 7. Remove seat from availableSeats
       ├─ 8. calculatePrice(seatNumber) [polymorphic]
       │
       ▼
Bus/Train specific pricing
       │
       │ 9. Return calculated price
       ▼
Create Ticket object
       │
       │ 10. Add to tickets collection
       ▼
Save to File (tickets.dat)
       │
       │ 11. Return ticket to UI
       ▼
Display ticket to user
```

---

## OOP Concepts Map

```
┌───────────────────────────────────────────────────────────┐
│                    ENCAPSULATION                          │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐      │
│  │    User     │  │   Ticket    │  │  Transport  │      │
│  │             │  │             │  │             │      │
│  │ - userId    │  │ - ticketId  │  │ - transportId│     │
│  │ - name      │  │ - price     │  │ - seats     │      │
│  │ + getName() │  │ + getPrice()│  │ + getSeats()│      │
│  └─────────────┘  └─────────────┘  └─────────────┘      │
└───────────────────────────────────────────────────────────┘

┌───────────────────────────────────────────────────────────┐
│               ABSTRACTION & INHERITANCE                   │
│                                                           │
│              Transport (Abstract)                         │
│                      │                                    │
│      ┌───────────────┴───────────────┐                   │
│      │                               │                   │
│    Bus                              Train                │
│  (extends)                        (extends)              │
└───────────────────────────────────────────────────────────┘

┌───────────────────────────────────────────────────────────┐
│                    POLYMORPHISM                           │
│                                                           │
│  Transport transport = new Bus(...);                      │
│  double price = transport.calculatePrice(5);             │
│  // Calls Bus implementation                             │
│                                                           │
│  transport = new Train(...);                             │
│  price = transport.calculatePrice(5);                    │
│  // Calls Train implementation                           │
└───────────────────────────────────────────────────────────┘

┌───────────────────────────────────────────────────────────┐
│                    COLLECTIONS                            │
│                                                           │
│  HashMap<String, Transport> ──→ Fast lookup by ID        │
│  HashMap<String, User> ──────→ Fast lookup by ID         │
│  ArrayList<Ticket> ───────────→ Dynamic list             │
│  ArrayList<Integer> ──────────→ Available seats          │
└───────────────────────────────────────────────────────────┘

┌───────────────────────────────────────────────────────────┐
│                    FILE I/O                               │
│                                                           │
│  Save: Object → ObjectOutputStream → File                │
│  Load: File → ObjectInputStream → Object                 │
│                                                           │
│  Serialization ⇄ Deserialization                         │
└───────────────────────────────────────────────────────────┘
```

---

## Sequence Diagram - Complete Booking Flow

```
User    TicketReservationApp    ReservationSystem    Transport    FileSystem
 │              │                      │                │             │
 │─Choose Option 3────────────────────>│                │             │
 │              │                      │                │             │
 │              │─Get TransportId──────>│                │             │
 │              │                      │                │             │
 │              │─Get SeatNumber───────>│                │             │
 │              │                      │                │             │
 │              │─bookTicket()─────────>│                │             │
 │              │                      │                │             │
 │              │                      │─isSeatAvailable()────────────>│
 │              │                      │<───true────────│             │
 │              │                      │                │             │
 │              │                      │─bookSeat()─────>│             │
 │              │                      │                │             │
 │              │                      │─calculatePrice()────────────->│
 │              │                      │<───price───────│             │
 │              │                      │                │             │
 │              │                      │─createTicket() │             │
 │              │                      │                │             │
 │              │                      │─saveData()────────────────────>│
 │              │                      │                │             │
 │              │<───ticket────────────│                │             │
 │              │                      │                │             │
 │<─Display Ticket───────────────────  │                │             │
 │              │                      │                │             │
```

---

## Component Interaction

```
┌──────────────────────────────────────────────────────┐
│                    Main Method                       │
│              creates & initializes                   │
└────────────────────┬─────────────────────────────────┘
                     │
        ┌────────────┴────────────┐
        │                         │
        ▼                         ▼
┌──────────────┐         ┌────────────────┐
│   Scanner    │◄────────│ ReservationSystem │
│  (User Input)│         │   (Core Logic)    │
└──────────────┘         └────────┬───────────┘
                                  │
                     ┌────────────┼────────────┐
                     │            │            │
                     ▼            ▼            ▼
              ┌──────────┐ ┌──────────┐ ┌──────────┐
              │ Transport│ │   User   │ │  Ticket  │
              │   Map    │ │   Map    │ │   List   │
              └──────────┘ └──────────┘ └──────────┘
                     │            │            │
                     └────────────┼────────────┘
                                  ▼
                          ┌──────────────┐
                          │  File System │
                          │ (Persistence)│
                          └──────────────┘
```

---

## Memory Structure

```
Heap Memory
├── ReservationSystem instance
│   ├── transports (HashMap)
│   │   ├── "BUS001" → Bus object
│   │   │   ├── transportId = "BUS001"
│   │   │   ├── source = "Mumbai"
│   │   │   ├── availableSeats = [1,2,3,4...]
│   │   │   └── busType = "AC"
│   │   │
│   │   ├── "TRN001" → Train object
│   │   │   ├── transportId = "TRN001"
│   │   │   ├── trainClass = "3A"
│   │   │   └── ...
│   │   └── ...
│   │
│   ├── users (HashMap)
│   │   ├── "USER0001" → User object
│   │   │   ├── userId = "USER0001"
│   │   │   ├── name = "John Doe"
│   │   │   └── ...
│   │   └── ...
│   │
│   └── tickets (ArrayList)
│       ├── [0] → Ticket object (TKT001000)
│       ├── [1] → Ticket object (TKT001001)
│       └── ...
│
└── TicketReservationApp instance
    ├── system (reference to ReservationSystem)
    ├── scanner (Scanner instance)
    └── currentUser (reference to User)
```

---

## Design Principles Applied

```
┌─────────────────────────────────────────────────────┐
│               SOLID PRINCIPLES                      │
├─────────────────────────────────────────────────────┤
│                                                     │
│ [S] Single Responsibility                           │
│     Each class has one reason to change             │
│     - User: manages user data only                  │
│     - Transport: manages transport logic only       │
│                                                     │
│ [O] Open/Closed                                     │
│     Open for extension, closed for modification     │
│     - Can add Flight without changing Transport     │
│                                                     │
│ [L] Liskov Substitution                            │
│     Subtypes can replace parent type               │
│     - Bus/Train can replace Transport anywhere     │
│                                                     │
│ [I] Interface Segregation                          │
│     No unused methods forced on clients            │
│     - Abstract methods only what's needed          │
│                                                     │
│ [D] Dependency Inversion                           │
│     Depend on abstractions, not concretions        │
│     - ReservationSystem uses Transport, not Bus/Train│
│                                                     │
└─────────────────────────────────────────────────────┘
```

---

**Use these diagrams during interviews to visualize your explanations! 📊**
